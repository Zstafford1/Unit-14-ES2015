//ES2015 Global Constants
const PI = 3.14;

//Both var and let can reassign and redeclare variables, but let cannot implement variable hoisting as it assigns variables in a block scope.
//Both var and const can assign variables, but const cannot reassign variables nor redeclare them; it also assigns variables in a block scope and cannot implement variable hoisting.
//Let and const can both assign variables, but let can also redeclare variables while const cannot; neither can implement variable hoisting.
//variable hoisting involves calling variables before declaring them.